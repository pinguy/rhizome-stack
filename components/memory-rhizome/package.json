{
  "name": "@local/memory-rhizome",
  "version": "0.1.0",
  "type": "module",
  "description": "OpenClaw memory plugin that blends core Markdown memory with RhizomeML FAISS recall",
  "main": "./dist/index.js",
  "scripts": {
    "build": "npx --yes esbuild index.ts --bundle --platform=node --format=esm --packages=external --outfile=dist/index.js && cp rhizome_memory_server.py dist/rhizome_memory_server.py"
  },
  "files": [
    "dist",
    "openclaw.plugin.json"
  ],
  "dependencies": {
    "@sinclair/typebox": "0.34.48"
  },
  "openclaw": {
    "extensions": ["./dist/index.js"]
  }
}
